# --------- pytorch --------- #
torch==2.3.1
torchvision==0.18.1
lightning==2.3.3
torchmetrics==1.4.0.post0

# --------- hydra --------- #
hydra-core==1.3.2
hydra-colorlog==1.2.0
hydra-optuna-sweeper==1.2.0
optuna==2.10.1            # pinned for hydra-optuna-sweeper; SA2.0 compat via src/utils/optuna_patch.py

# --------- loggers --------- #
wandb==0.22.3

# --------- others --------- #
rootutils==1.0.7       # standardizing the project root setup
rich==13.7.1           # beautiful text formatting in terminal

# NOTE: genet 0.15.1 requires tensorflow < 2.10.0 and protobuf <= 3.20.3.
# However, Python 3.11 requires tensorflow >= 2.12.0 and the system enforces protobuf 4.x.
# tensorflow 2.15.0 with protobuf 4.24.4 is verified to work with genet's pre-trained models.
tensorflow==2.15.0
protobuf==4.24.4
numpy==1.26.4          # for genet
pandas==2.1.4
pyarrow==17.0.0
# genet==0.15.1 (Install separately: pip install genet==0.15.1 --no-deps)
scikit-learn==1.4.2